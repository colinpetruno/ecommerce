<%= simple_form_for [product, OrderItem.new], remote: true do |f| %>
  <%= f.association :sku,
    as: :select,
    collection: product.skus,
    include_blank: false %>
  <%= f.submit "Add To Cart", class: "btn-block" %>
<% end %>
